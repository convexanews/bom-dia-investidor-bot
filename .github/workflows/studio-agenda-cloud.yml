name: BDI Studio - Agenda na nuvem

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: studio-agenda-cloud
  cancel-in-progress: false

jobs:
  publicar-agendados:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Verificar se há publicação vencida
        id: queue
        shell: bash
        run: node -e "const a=require('./studio-cloud-agenda.cjs'); console.log('due='+(a.dueCloudItems(a.readLocalCloudAgenda()).length>0))" >> "$GITHUB_OUTPUT"
      - uses: actions/setup-node@v4
        if: steps.queue.outputs.due == 'true'
        with:
          node-version: 22
          cache: npm
      - name: Instalar dependências
        if: steps.queue.outputs.due == 'true'
        run: npm ci
      - name: Processar publicações vencidas
        if: steps.queue.outputs.due == 'true'
        id: agenda
        continue-on-error: true
        env:
          IG_TOKEN: ${{ secrets.IG_TOKEN }}
          IG_ACCOUNT_ID: ${{ secrets.IG_ACCOUNT_ID }}
        run: node processar-agenda-studio.cjs
      - name: Salvar agenda e histórico
        if: always() && steps.queue.outputs.due == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- studio-agenda-cloud.json noticias-postadas.json relatorio.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Processa agenda cloud do Studio"
          for tentativa in 1 2 3; do
            git pull --rebase origin main && git push origin HEAD:main && exit 0
            sleep $((tentativa * 2))
          done
          exit 1
      - name: Informar falha de publicação
        if: steps.agenda.outcome == 'failure'
        run: exit 1
